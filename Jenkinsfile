pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'date'
                sh 'whereis mvn'
            }
        }
        stage('Test') { 
            steps {
            sh 'date'
                            }
        }
        stage('Deploy') { 
            steps {
               sh 'date'
            }
        }
    }
}

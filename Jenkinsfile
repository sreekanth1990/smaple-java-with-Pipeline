pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'date'
                sh 'whereis mvn'
            }
        }
        stage('Test') { 
            steps {
            sh 'date'
                sh 'which mvn'
            }
        }
        stage('Deploy') { 
            steps {
               sh 'date'
            }
        }
    }
}
